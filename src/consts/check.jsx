import {area} from "./sizes"
export function CheckResult() {
    // Найти input и прочитать значение
    // P.S. получить элемент html по имени (из свойств name) const result = document.getElementsByName("result"); 
    // получить значение введенное пользователем let res = result[0].value;
    // т.к. переменная с input является string, для корректности перевести в float
    // сравнить введенное пользователем значение с правильным
    // возвращает true или false
}